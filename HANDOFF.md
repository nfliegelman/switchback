# HANDOFF.md

You are an AI picking up this project in a fresh conversation. Read this file first, then ROADMAP.md, then skim SPEC.md and SCOPE.md. Last updated 2026-07-07.

## What this project is

Switchback: a permit-aware backpacking trip finder built on recreation.gov data. The name is the trail's answer to terrain that is too steep for one day, which is what the solver does to itineraries. It started as a Tkinter GUI that dumps wilderness permit availability to Excel and is growing into: a constraint-based itinerary recommender (daily miles, gain, grade bounds), an interactive "adventure mode" frontier explorer, camp ratings, and GPX or CalTopo handoff. Owner: Noah, construction estimator, strong Python builder (has shipped multiple quant models with GitHub Actions and Pages dashboards), plans PNW and Rockies trips.

## Owner conventions. Non-negotiable. Violations are noticed.

1. NO em dashes or en dashes anywhere: chat, docs, code comments, filenames. Use commas, colons, periods, hyphens. Run a dash grep before every handback.
2. Surgical edits only. Never rewrite files wholesale when a targeted change works.
3. Every handback is the COMPLETE repo as a zip, with full files, nothing truncated.
4. Never return state JSON files (runtime or model state). Data snapshots and config are fine; the availability fetch stays live in scripts.
5. Style: quantified, evidence-based, concise. Tables for comparisons. Distinguish critical, moderate, and negligible differences. Give a recommendation, the strongest argument against it, and challenge the premise when warranted.
6. Reddit protocol for any community-sentiment claims: only with actual threads found, links provided, quotes cited, counts given. If none found, say so explicitly and label synthesis as synthesis.
7. Verify prices and market claims against real sources before repeating them.
8. At most one clarifying question per turn; answer what can be answered first.

## Repo inventory

- switchback/: the engine package. api.py (stdlib-only data layer, fetch_availability_rows, get_permit_content), config.py (profile loader), extract.py (M1 park extractor), __main__.py (CLI: search, availability, profile, extract). Landed v1.1.0-v1.2.0.
- parks/: extracted park datasets (rainier.json, glacier.json, schema 2 with features). parks/manual_coords.json holds the coordinate queue (4 Glacier camps). parks/.osm_cache/ is gitignored fetch cache.
- switchback/features.py: M2 pipeline (coordinate fill, NHD hydro joins, elevation, trailhead distance) with prefetch mode.
- switchback/graph.py and parks/edges/: M3 route graphs with per-edge sources.
- switchback/solver.py and tests/test_solver.py: M4 itinerary engine and its synthetic parity oracle.
- switchback/scoring.py, scoring.json, parks/ratings.json, tests/test_scoring.py: M5 ranking layer.
- CLI now: search, availability, extract, features, graph, trips, export, profile.
- switchback/report.py: shared trip renderer for CLI and GUI. switchback/gpx.py plus tests/test_gpx.py: M7 exporter and invariants.
- switchback/history.py plus tests/test_history.py: M8 logger (SQLite, gitignored) and demand derivation.
- switchback/watch.py plus tests/test_watch.py: M9 watcher; telegram.json.example is the config template.
- tests/test_shapes.py: M10 invariants (classifier shapes, bounds, endings monotonicity).
- CLI complete: search, availability, extract, features, graph, trips, export, watch, history, profile.
- profile.json: the saved effort profile. Config, not state; edit freely.
- requirements.txt: GUI extras (customtkinter, openpyxl); the engine is stdlib-only.
- switchback_gui.py (was permit_finder_gui.py): original working GUI. Search permits, load divisions, fetch monthly availability (6 threads), classify (Reservable, Walk-up only, Full, Not released, Hidden), styled xlsx export. Renamed, window title updated, em dashes purged 2026-07-07; logic untouched.
- Switchback.bat (was Permit_Finder.bat): Windows launcher, updated for the rename.
- FUTURE.md: first roadmap. SUPERSEDED by ROADMAP.md and BACKLOG.md; kept for history and the monetization gates G1-G3.
- SPEC.md (was TRIPFINDER_SPEC.md): full engine design. Section 10 is adventure mode (virtual origin and sink, backward feasibility, endings_remaining, trip types, freshness re-check).
- SCOPE.md: three-bucket feature triage and the core principle: a route is an ordered camp list, geometry always derived.
- ROADMAP.md: the V1 build tracker, milestones M0-M10 with checkboxes. THE active plan.
- BACKLOG.md: categorized future features with the owner's direction per item.
- README.md: public-facing overview for GitHub, current capabilities and quickstart.
- CHANGELOG.md: version history; owner bumps versions per ROADMAP.md scheme.
- .gitignore: excludes permit_exports/, caches.
- caltopo_export.py: works today. Dumps any permit's camps and trailheads as CalTopo-importable GeoJSON, colored by type. Run: python caltopo_export.py <permit_id>.
- caltopo_Mount_Rainier_...geojson: generated sample, 245 features.
- belly_river_graph.json: hand-built Glacier Belly River graph. Every edge has miles, gain_ab, loss_ab, and a src citation; est flags mark topo estimates.
- belly_river_adventure.py: WORKING adventure-mode demo. Fetches live availability for 10 Belly River camps, runs the frontier walkthrough and batch enumeration, falls back to planning mode when advance inventory is nearly gone.

## Validated technical facts (probed live, dates noted)

- Rainier wilderness permit id 4675317: 190 divisions, 186 with real lat/long; entrances array has 60 trailheads with lat/long and has_parking. (2026-07-07) CORRECTION 2026-07-08: division-side entry_ids/exit_ids are empty in both parks, and entrance-side division_ids is PERMISSION data, not proximity (Rainier: 21 sparse 1:1 links; Glacier: 52 of 53 entrances linked to roughly 130 camps each, 6788 links). Do not use it for graph edges.
- Glacier wilderness permit id 4675321: 213 divisions, coordinates all 0,0 (fill from NPS GIS or OSM name-join); divisions include Administrative, Hitch Rail, and Spike variants that must be filtered; owner's example camps exist as ELF, ELH, POI, MOJ, MOL; trailheads BRE Belly River Trail and IPE Iceberg/Ptarmigan Trail exist. (2026-07-07)
- Availability endpoint: GET /api/permititinerary/{permit}/division/{division}/availability/month?month=M&year=Y&commercial=false. Sum remaining across payload.quota_type_maps; payload.bools is the fallback shape. remaining can be NEGATIVE (admin holds); clamp to 0. Browser User-Agent header required on all rec.gov calls.
- Search endpoint: /api/search?q=...&entity_type=permit. permitcontent: /api/permitcontent/{permit}.
- child_distances is empty for Rainier and Glacier; camp-to-camp mileage comes from official park tables or OSM plus DEM compute.
- USGS NHD hydrography works via hydro.nationalmap.gov/arcgis/rest/services/nhd/MapServer: discover layer ids from the service json, query with ftype IN (390,436) for waterbodies and ftype IN (460) for flowlines, envelope geometry, f=geojson, 1000-row pages. Read properties case-insensitively; gnis_name can be blank spaces. (2026-07-08)
- Overpass congestion playbook: mirrors saturate together; use pacing, per-query mirror rotation, fine tiles, and the parks/.osm_cache disk cache. For heavy geometry layers prefer NHD. (2026-07-08)
- Sandbox lesson: background processes do NOT survive between tool calls. Long fetches must run as time-boxed foreground bites against a disk cache. (2026-07-08)
- Glacier ground truth: after 16 exclusion reasons the included set is 66 designated backcountry camps, matching the park's real inventory. Name-matching lessons: foot/head/lower/upper are disambiguators and must survive normalization; saint normalizes to st; Zone N divisions are undesignated areas, not points. (2026-07-08)
- Scoring facts: weights and prior coefficients live in scoring.json; personal ratings in parks/ratings.json override priors; the crowd term reads parks/demand.json when M8 creates it. QA note for the ratings pass: Dick Creek Camp came back lake=true from NHD (verify: possible small pond); the top Rainier day legs carry more real gain than est shows per the M3 caution. (2026-07-08)
- Rainier's division `size` field ships blank; per-camp capacity comes from availability totals (quota_type_maps), never from size. (2026-07-08, written down 2026-07-13 sweep)
- Wonderland transcription source: Where The Road Forks camp guide, cross-checked against Wonderland Guides day sums and Visit Rainier cumulatives; variances flagged per edge in parks/edges/rainier_edges.json. Rec.gov abbreviates South/North as So./No., handled in norm(). Endpoint-delta est gains understate passes about 40 percent (Longmire>Klapatche est 3658 vs GPS 6450); the DEM pass replaces them. (2026-07-08)
- Mileage sources used for Belly River: PNTA Glacier permit planner PDF (corridor mileages; ELF is 3.4 off the corridor at mile 6.1), enjoyyourparks Red Gap page (Many Glacier to ELF 9.8 mi, Redgap to ELF 4.6 mi and about 2,500 ft), roamingbearmedia (pass to Poia 5.7 mi, 1,754 ft), repicjourney (Poia TH to Poia 6.4 mi), Glacier Guides (TH to tunnel 10.6 mi round trip, 2,300 ft).
- OSM has the Wonderland Trail as a route relation and 75 camp_site features in the Rainier bbox. AllTrails has NO public API and enforces against scrapers (community scraper shut down Jan 2026); official AllTrails MCP exists in the owner's Claude for read-only lookups; content is never stored, but factual data points with src attribution are allowed sparingly per the 2026-07-13 policy in BACKLOG.
- Live scarcity snapshot 2026-07-07, party of 2, Jul 10 to Sep 22: exactly one advance-bookable 3-night Belly River chain (start Sept 22, Chief Mountain, GAB > GLF > GAB). STALE IMMEDIATELY; re-fetch, never quote this as current.

## Decisions log (dated 2026-07-07 unless noted)

- Trip types V1: loop, out_and_back, lollipop. Classifier on the closed walk: no repeated edges = loop; full retrace = out_and_back; repeats forming one contiguous stem = lollipop.
- Shuttle: PARKED by owner, future toggle.
- Same-trailhead return only in V1. Virtual origin over all entry trailheads defers the trailhead choice; virtual sink mirrors it.
- Backward feasibility always on: never show a frontier option without at least one valid completion; show endings_remaining per card.
- Batch top-N is the default entry point; adventure mode is the explore and modify layer.
- Ratings: lazy (rate only camps that surface), personal rating overrides computed prior, per-park percentile normalization. Priors currently hand-set in belly_river_graph.json, labeled DRAFT.
- Demand and crowding comes only from our own scan history (M8 logger); no external source exists.
- Walk-up inventory excluded from bookable results by default; planning mode relaxes availability for walk-up strategy.
- GPX handoff to AllTrails, Garmin, Gaia, CalTopo is the integration surface. CalTopo interop over imitation.
- 2026-07-07 (round-one product answers): day-to-day interface target is a LOCAL WEB APP, chosen by the owner's criterion of the most interactive, intuitive, visually appealing UI; terminal and the tkinter GUI are not the daily driver. Architecture note: browsers cannot call recreation.gov (CORS), so the web UI is a local FastAPI backend proxying the API plus a no-build-step Leaflet frontend in a single HTML file. Whether the web UI ships inside v2.0.0 or immediately after as v2.x is pending an explicit scope decision. Operating model: on-demand planning plus per-trip watch mode, no always-on scanner. No anchor trip; build in ROADMAP order. New DIRECTED backlog item: Permit Difficulty Index (BACKLOG.md category 4).
- 2026-07-08: TripFinder.bat added as a stopgap double-click launcher for the trips CLI. Owner had been double-clicking Switchback.bat expecting new features and correctly finding only the old GUI, since no M6 GUI trigger exists yet. Do not treat this as M6; M6 is still adventure mode plus a real Find Trips button inside switchback_gui.py.
- 2026-07-13 (v2.19.0 gains): elevation source order is OpenTopoData ned10m then open-elevation, both meters times 3.28084, one second between calls; EPQS stays dead from this egress. NPS unit boundaries probe result in the session wrap; item 2 starts there, item 3 starts from the EXPERIMENTS.md merge design.
- 2026-07-13 (v2.18.0 floor sprint): EPQS blocked from this egress, use OpenTopoData or open-elevation next; containment-fraction audit (nodes inside boundary rings) is the standing tool for suspicious area networks; Conundrum TH is oracle-anchored at the road terminus, do not move it again, the spur waits on an owner GPX trace.
- 2026-07-13 (v2.17.0 sweep): EPQS elevation endpoint unreachable from the container, retry before assuming gains impossible; IPW dual merge design lives in EXPERIMENTS.md; WA hanger lesson: rerun transient Overpass failures before engineering around them.
- 2026-07-13 (v2.16.0 phase B): edges files use the miles key and ENT:<name> refs (copy lena_edges.json exactly); policy none on a camp is the whole synthetic availability story; pilot parks stay out of the app until the EXPERIMENTS.md limitations clear.
- 2026-07-13 (v2.15.0 CO wave): all 27 planned USFS wildernesses built; the boundary service registers Cache La Poudre with a capital La (atlas official field fixed); remaining CO planned rows need non-wilderness boundary sources (BLM, NPS Black Canyon, monuments, state, long-trail corridors).
- 2026-07-13 (v2.14.0 WA wave): MIRRORS_FLOOR is advisory now (best response kept for sparse areas); batch area builds at three or fewer per command, the execution window kills more; wondermountain and glacierview hang a full mirror timeout on tiny bboxes, retry later or shrink the pad.
- 2026-07-13 (v2.13.0 national atlas): parks/atlas.json replaced colorado_atlas.json as the single source (rows carry a state field); render_atlas writes one MD per state; areas.py reads atlas.json. WA wilderness rows carry official USFS names so python -m switchback area <slug> works on each; Goat Rocks proved it (755 KB, PCT-corridor density is why WA files run larger).
- 2026-07-13 (v2.12.0 dispersed pilot): phase A PASSED, details in EXPERIMENTS.md; the 400 m snap gate is mandatory (an ungated run matched a Ruby Lake 3.4 km off the network); the Needleton anchor snaps 233 m from the bridge; grade every future drainage against published mileage before wiring availability.
- 2026-07-13 (v2.11.0 graphs and rubber band): area junction graphs MUST be built on raw geometry and simplified per edge afterward, Douglas-Peucker removes the shared vertices where ways cross; length filters go at component level (drop components under 0.3 mi), never per way; _bridge_components heals gaps to 200 m and the measured gap census for Weminuche was 103 m, 179 m, then 1.1 km and beyond, so remaining islands are geography and the client routes within the network you touch, which is correct. Area file format v2 is nodes plus edges [a, b, mi, geometry]; the board loader falls back to the old trails key for stale PWA caches. Drag-routing disables map.dragging while armed. GPX export of drawn routes is queued in BACKLOG.
- 2026-07-13 (v2.10.1 launchers): STANDING POLICY: the owner is not tech savvy and wants exactly two entry points, the phone app and one double-click on Windows; never advertise console interactions in user-facing docs, terminal lives only under an Advanced heading. Switchback.bat pattern: cd to its own folder, check python with a friendly install message, pip install fastapi uvicorn quietly on first run, start pythonw uvicorn on port 8756, open the browser after two seconds; a second launch fails to bind silently and the browser opens to the existing server, which is the intended behavior. Quit is POST /api/quit via the footer link. TripFinder.bat and SwitchbackMap.bat are gone; do not resurrect prompt-driven consoles.
- 2026-07-13 (v2.10.0 PWA): iOS takes the home screen icon ONLY from the apple-touch-icon link, not the manifest. The service worker lives at docs/board/sw.js and scopes the board path, but it intercepts same-origin fetches to ../areas/ fine because scope binds to the page, not the target. Cache policies: shell precached and versioned (bump the SHELL constant on any board HTML change or clients keep the old page), board.json network first, areas stale while revalidate. Rubber band route builder spec is in BACKLOG with the two phase plan.
- 2026-07-13 (v2.9.0 Colorado statewide): boundary source is the USFS EDW_Wilderness_01 service, field name is lowercase wildernessname, Weminuche arrives as 15299 points and simplifies at 150 m. Trail areas have no graph nodes, so the OSM fetch cannot use the coverage validator; the guard is a 25-way floor plus the government layer as an independent complement. Display files live in docs/areas with an index.json manifest regenerated on every build. Chicago Basin is the Needle Creek drainage inside Weminuche, not a separate area. Area trail miles count every mapped foot way in the padded bbox and run above official system-trail figures by design. parks/colorado_atlas.json is the source of truth for COLORADO.md; edit the JSON, rerun the atlas command.
- 2026-07-13 (v2.8.0 gov synthesis): GOV_SERVICES registry = TNM transportation layer 37 (carries the NPS park trails) and USFS EDW TrailNFSPublish layer 0; ArcGIS queries MUST use JSON envelopes, comma envelopes return zero features silently; quadtree tiling above 1900 features; the gov fetch is lazy (only when OSM fails) and cached as gov_<slug> files; polylines carry a src field (osm or gov). POLICY: a straight that fails on both independent datasets counts as a confirmed absence, not a hole. Maroon Zone sits on the valley-side trail (Crater leg fixed, East Fork leg straight); Maroon to North Fork displays an 11.8 mi line against 7.19 stored miles, Elk Range zone-map QA still queued.
- 2026-07-13 (v2.7.1 seam fix): geometry polylines are stored in canonical sorted-key orientation and path_for depends on that invariant; the migrator flipped 44 of 85. Board generation prints a seam guard (straight or seam hops over 0.5 mi); expect zero unless a top route genuinely crosses a fallback edge. LESSONS: post-generation verification must run after the final regen (the 174 point check had validated an earlier board), and orientation assumptions are coin flips until asserted.
- 2026-07-13 (v2.7.0 trail geometry): parks/geometry/<slug>.json holds simplified polylines keyed by sorted node-id pairs ("a|b"); coarse true means shape trusted, mileage not; the ODbL attribution string ships in every geometry file and must remain on every map surface. Overpass playbook: fetch_validated walks the mirror list and accepts only responses whose trail vertices cover 70 percent of graph nodes within 800 m (partial mirror extracts pass size floors, coverage caught them); caches carry a _coverage stamp and self-invalidate below it. STANDING COORD-REPAIR RECIPE: anchor camp and entrance coords to OSM named features via Overpass nwr name queries, or use the terminus trick (snap to the nearest vertex of the big trail component); this session it caught errors of 1.1 to 8.7 km at Lena, Teton, Enchantments, Maroon Bells, and Indian Peaks. MILES POLICY: geometry-measured mileage replaces curated figures only for recall-grade networks (tetoncrest, indianpeaks, and the maroonbells trunk pass legs); published-source parks keep their figures. All trip lines (map, board, GPX) flow through geometry.day_path; layover days emit the stationary point. The Pages board draws trail lines only after v2.7.0 lands on GitHub and a board run commits a fresh board.json.
- 2026-07-13 (v2.6.0 gate catches, post-build): the dash gate caught rec.gov's own en dash in the Indian Peaks permit name leaking into generated PARKS.md (name normalized in the dataset, coverage generator now sanitizes dashes defensively). The DEM skip diagnosis exposed a real correctness bug: RMNP "(Winter)" variants collide with summer site names under norm(), winter sorts first, so a Wild Basin edge was resolving to the WINTER North St. Vrain division. All 26 winter variants are now excluded (seasonal modeling queued in BACKLOG); resolution re-verified onto summer divisions and the live proof re-run. LESSON: after building any park graph, assert every edge ref resolves to the intended division id, not just to any node.
- 2026-07-13 (v2.6.0 Colorado wave): PERMITS: maroonbells 4675333, indianpeaks 4675318 full-season (4675319 is the separate 3-days-in-advance inventory, dual-permit merge queued), sanddunes 4675316, rmnp 4675320; Black Canyon has NO rec.gov permit, holdout. MODELING: site-level destinations (Crater 11 / Conundrum 20 / Capitol 9) use cluster junctions with 0.1 mi spokes GENERATED from the park json so the solver books any open site; IPW group divisions are 0.1 mi twins of their zones; RMNP edge refs must use EXACT site names including snow-free suffixes (fuzzy matching collides with Group/Stock variants). DATA HONESTY: RMNP shipped 3 retired bivy sites as active (excluded, reason retired); Sand Dunes payload cloned the HQ coordinate onto all 8 sites; rec.gov zone coords junk 3-for-3 on forest permits (Enchantments, IPW zero coords, Dunes cloned); MB dispersed zones had no coords (curated); one RMNP payload coord was incomplete (Thunder pair curated). GAINS: pass arithmetic in the Elks and IPW (named passes: West Maroon 12,500 / Frigid Air 12,415 / Trail Rider 12,420 / Buckskin 12,462 / Pawnee 12,541 / Buchanan 11,837 / Arapaho 11,906 / Devils Thumb 11,747), DEM line in Wild Basin and the dunes. MB "Areas Outside of Permit Zones" is a real division left un-graphed pending semantics check. Sand Ramp sites ran thin in the Sept live check (possible Medano-corridor closure pattern, verify). Day hikes now ignore sub-mile spokes (Scorer.MIN_DH_RT_MI). Live proofs: MB 24 Sept itineraries incl the Four Pass Loop as a 3-nighter, IPW 16, RMNP Wild Basin 301 across 64 routes.
- 2026-07-13 (v2.4.0 DEM + Teton Crest, v2.5.0 Enchantments + PDI): GAIN METHOD TIER (graph uses gain_ab regardless; the tier is provenance): sourced > alltrails_derived / pass_arithmetic_v1 > dem_line_v1 > endpoint. dem CLI fills est edges, respects dem_exempt, flags ridge_suspect when sampled terrain exceeds both endpoints by 1,200+ ft. BENCHMARK: Longmire-Klapatche 3,658 to 6,870 vs GPS 6,450 (43 pct low to 7 pct high). DEM INVALID across Teton and Stuart Range canyon walls (lines cross peaks); those edges use pass arithmetic from named pass elevations and are exempt. QA queue: Rainier Maple Creek-Paradise River and White River-Summerland show DEM overshoot vs plausible trail gain; chain-level accuracy holds, per-edge check queued. FACTS: gteton permit 4675342, 129 areas, ZERO payload coords (all Crest zone coords curated); enchantments advanced lottery 233273 (daily lottery 445863 separate, future dual-permit region), rec.gov zone centroids are junk (Snow Zone 14 mi off, Stuart cloned onto Colchuck), curated. Teton Crest region live: 118 Sept itineraries; Enchantments honest zero (post-lottery sellout). PDI: history pdi, 30-sample floor, null components until data depth, 28 camps qualified day one, board.yml refreshes parks/pdi.json daily. Two tests unpinned from pre-DEM gain values to their real invariants.
- 2026-07-13 (v2.2.0 regions + v2.3.0 board, built back to back at owner's "next two versions"): REGION SCHEMA: parks/<slug>.json with region true, members[].include (dict entries; extra keys override member records because rec.gov's Upper Lena coord is 18 mi off), overlay camps with policy none/fcfs. load_park merges; graph nodes carry permit_id and policy; solver.fetch_for_graph is the ONE availability choke point now (groups by permit, synthesizes always-open 99s for permitless camps) and cmd_trips, web, and board all use it; watch still fetches single-permit, fine since synthetic camps never transition, region watching covers reservation camps only. FIRST-COME HONESTY: report, map tooltips, frontier, and board all label policy none/fcfs nights first-come, never Reservable. OLYMPIC FACTS: permit 4098362, 297 included camps, 146 coords free from payload, Upper Lena division 4098362011 (group site 012 excluded), features pass queued as its own session. BOARD: relative windows (start_in_days, span_days), board.yml daily cron commits docs/board plus data/history.sqlite snapshot plus demand.json only on change, needs Pages enabled on main /docs. Live observation: mid-August Glacier Belly is fully booked, board reports zero honestly. Lena coords are curated approximations, QA queued.
- 2026-07-13 (v2.1.0 the map, owner overrode the gate): owner consciously started the web UI before giving scoring reactions (phone-only until ~July 19, front-loading Claude-heavy builds; calibration folds into v2.1.x). switchback/web.py (FastAPI, create_app(fetch_fn=) injectable like watch.py, 2-minute availability cache) plus web_index.html (single-file Leaflet, no build step) plus SwitchbackMap.bat plus tests/test_web.py (offline via injected fetcher). Proven: offline suite green, live boot served the map and a real Glacier trips call returned the Belly chain with day paths. Data fact: park-json feature flags are FLAT (water_lake_flag Y/N) keyed by camp id; there is no nested features dict; the offline test caught the mapper assuming otherwise. GitHub uploads pending since ~v2.0.2: the cloud watcher is INERT until the owner uploads the latest zip; one upload of the newest zip catches everything up. The v2.x line is planned in ROADMAP.
- 2026-07-13 (cloud watcher shipped, v2.0.7): watch mode deployed as .github/workflows/watch.yml on the owner's established Nimbus/Drizzle pattern (Actions cron plus mobile workflow_dispatch). Standing watches live in watch_config.json (enabled flag; disabled cron runs no-op in seconds); ad-hoc watches take dispatch inputs. State and history persist via actions/cache with rolling restore-keys: exactly-once is SOFT in the cloud (cache eviction can duplicate one alert, never miss one). Secrets: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID. Browser-upload caution: confirm the .github folder actually made it up; the Actions tab listing "Switchback watch" is the check. Cron pauses after 60 days of repo inactivity.
- 2026-07-13 (first test drive, from a phone): steps (a) and (b) ran in-chat against the live container. Found a silent-filter bug: profile.json trip_type "loop" suppressed 635 of 639 Rainier routes with no disclosure (top score 0.145 vs 1.040 unfiltered). Fixed in v2.0.6: the report always names the active trip type and warns when a filter is discarding shapes. LESSON: any filter that can hide results must announce itself; audit future filters (codes, via, dates) for the same silence. Owner's profile still says loop; ask before flipping it to any.
- 2026-07-13 (AllTrails integration matrix): full tool inventory done at the owner's request; the decided matrix lives in the BACKLOG AllTrails item. Headline discovery: trail_traffic labels and popularity sorts on the search tools give a crowd and seasonality proxy with no details call needed. get_trail_details approval has failed to register three times (prompt must be tapped mid-call); UGC counts pending until it lands.
- 2026-07-13 (AllTrails policy refined): owner challenged the blanket link-only rule and was right. New policy in BACKLOG: facts with attribution sparingly via the official MCP, content never stored, bulk never, the app never calls AllTrails directly. Proven live in-chat: Lena corridor pulled, seed edge figures recorded in BACKLOG for the regions pilot. One get_trail_details call awaited an approval the owner did not grant this session, so UGC volume stats (the crowd-proxy input) are still unharvested; ask before assuming that approval next time.
- 2026-07-12 (regions + coverage): owner requested cross-boundary linking (Lower Lena NF, no permit, chaining to Upper Lena NP permit): recorded as the DIRECTED regions item in BACKLOG with the FCFS honesty rule (permitless nights are labeled first-come with no guarantee, never Reservable) and the Lena corridor as pilot. Owner also asked whether to keep a rolling coverage list or add everything at once: answer recorded in PARKS.md, which is GENERATED by python -m switchback coverage --write, never hand-edited; Tier 0 (availability finder) already covers every rec.gov permit, Tier 1 datasets are batchable, Tier 2 graphs are demand-driven.
- 2026-07-12 (doc audit): the owner flagged that conversational decisions were not reliably landing in the docs, and was right: the park/forest expansion plan lived only in chat (now in BACKLOG), and SPEC still showed grade_pct with no deferral note (now annotated). NEW STANDING CONVENTION: any strategy decision made in conversation gets written into the relevant doc in the same turn, no exceptions.
- 2026-07-12 (owner's publish workflow): the owner pushes via the github.com browser by uploading the NEWEST zip only; no history replay. The web uploader never deletes, so renamed files become ghosts: before committing, delete any stale filenames from the repo file list. GitHub therefore reflects whichever zip was last uploaded, and may lag these handbacks; do not assume the remote matches the latest zip. Repo name: lowercase switchback.
- 2026-07-12 (ladder complete): M8, M9, M10 landed as v1.9.0, v1.10.0, v2.0.0. History hook lives in api.fetch_division_month and must stay fail-silent. Watch alert logic is the pure WatchState machine in watch.py; do not put IO in it. Telegram config: telegram.json (gitignored) or env vars; owner has an existing digest bot token to paste. Classifier truth recorded: Sunrise Northern Loop is a lollipop, White River full Wonderland is the pure loop. Owner smoke tests outstanding: GUI Find Trips click, GPX import into CalTopo or AllTrails, one real Telegram send after pasting the token. Next major build: the v2.1 web UI (map, ranked route layers, adventure mode; engine functions ready). License remains the owner's call, MIT suggested.
- 2026-07-12 (reshuffle): adventure mode interaction deferred to the v2.1 web UI at the owner's direction; M6 slimmed to the GUI Find Trips trigger plus --via route steering, both landed as v1.7.0. M7 (GPX plus CalTopo extras) landed as v1.8.0. The GUI button follows the file's existing queue pattern and compiles, but no display exists in this sandbox: owner must smoke test the click. GPX import into AllTrails/CalTopo is likewise an owner smoke test; the Sept 22 sample file ships for that purpose. Next: M8.
- 2026-07-12: checkpoint resolved, owner said proceed with the roadmap. Requested and received basecamp day-hike enrichment (v1.6.2): layover days surface ranked day-hike options from the graph; destinations ignore availability by design (the Lower Lena / Upper Lena pattern: base where quota exists, day-hike to the sold-out gem). Destinations are graph camps only in v1; POI nodes (passes, lookouts, non-camp lakes) queued in BACKLOG. Next action remains M6, whose Find Trips button should carry these layover notes through.
- 2026-07-08: owner paused active building to test-drive the engine and evaluate whether the product direction matches their vision before continuing past M5. This is a legitimate checkpoint, not a stall; the brain (M0-M5) is done and expensive to unwind, the remaining CLI/export/alert milestones (M6-M9) are cheap wrappers around it and safe to reorder or defer. If resuming a fresh session mid-checkpoint, ask whether testing happened and what came of it before continuing the roadmap.
- 2026-07-07 (round-two answers): engine first; the web UI ships as v2.1 immediately after v2.0.0. A thin Find Trips trigger in the tkinter GUI is in scope at M6 (1-2 h, exercises the same engine API the web UI will call); deep tkinter trip UI rejected as throwaway. Effort limits: ONE saved default profile in profile.json (party 2, pref 9 mi / 2200 ft, max 13 mi / 4000 ft, trip_type loop, walk-up excluded), overridable per run. Grade deferred until the DEM pass.
- 2026-07-07: M0 landed as v1.1.0. Engine functions moved verbatim to switchback/api.py; fetch_availability_rows is the shared row builder for CLI, GUI trigger, and web UI. CLI verified live against Glacier ELF.
- 2026-07-07: App named SWITCHBACK. Version scheme: v1.0.0 = state at first GitHub upload; each completed roadmap milestone bumps the minor version; all ten = v2.0.0. The repo is now fully dash-clean including code, and every handback must keep it that way.

## Known issues

- Negative remaining renders as "-1 sites open" in belly_river_adventure.py card output; clamp in M6.
- GPX export not built yet; CalTopo GeoJSON lacks rating and availability properties until M5 and M8 data exists.
- est-flagged gains in belly_river_graph.json pending a DEM pass; ELF to ELH and ELH to HEL distances are estimates pending the park table.

## Next actions

1. Ask the owner for a real date window, party size, and trip type; rerun belly_river_adventure.py live if a trip is imminent.
2. The M0-M10 ladder is COMPLETE at v2.0.0 (2026-07-12). The gate before the v2.1 web UI build is the owner's test drive, agreed 2026-07-12: (a) TripFinder.bat on Rainier, late September, 3 nights, gut-check the top route; (b) same run with --via steering; (c) export a route to GPX and import it into CalTopo or AllTrails; (d) paste the Telegram token into telegram.json and fire one --inject alert; (e) click Find Trips in the GUI once. Reactions, including messy ones, calibrate scoring.json before the UI calcifies on top of the engine. If resuming fresh, ask how the test drive went before building anything.
3. Park and forest additions follow the expansion plan in BACKLOG.md (first wave: Olympic, then Teton) and slot in independently of the web UI track whenever a real trip demands one. Exception rule: pull M9 (watch mode plus Telegram) forward if the owner wants Glacier cancellation alerts now; ELF, COS, ELH are the camps to watch.
3. On every handback: dash grep, full repo zip, keep this file updated (decisions log and validated facts grow; snapshots get re-dated).
